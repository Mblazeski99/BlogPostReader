<UserControl x:Class="BlogReader.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:BlogReader.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel, IsDesignTimeCreatable=True}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource PrimaryBorderColorBrush}"
                Padding="0 0 0 20" Margin="0 0 0 20">

            <TextBlock FontWeight="Bold"
                       Text="Latest" 
                       FontSize="28" />

        </Border>

        <ListView ItemsSource="{Binding BlogPostItems}"
                  ScrollViewer.CanContentScroll="False"
                  BorderThickness="0"
                  Grid.Row="1">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource NoHoverListViewItemStyle}">
                    
                    <Setter Property="Height" Value="280" />
                    <Setter Property="Margin" Value="0 0 0 20" />
                    <Setter Property="BorderThickness" Value="0" />
                    
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>

                    <Grid>

                        <Grid.RowDefinitions>
                            
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />

                        </Grid.ColumnDefinitions>

                        <Border Padding="10 5" Background="{StaticResource PrimaryButtonColorBrush}">
                            
                            <TextBlock Text="{Binding SourceName}"
                                       FontWeight="DemiBold"
                                       TextAlignment="Left"
                                       Foreground="#fff" />
                            
                        </Border>

                        <TextBlock Text="{Binding Title}">

                            <TextBlock.Style>
                                <Style TargetType="TextBlock">

                                    <Setter Property="Grid.Row" Value="1" />
                                    <Setter Property="Grid.ColumnSpan" Value="2" />
                                    <Setter Property="TextAlignment" Value="Left" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Margin" Value="0 10 0 0" />
                                    <Setter Property="Cursor" Value="Hand" />
                                    <Setter Property="FontSize" Value="24" />

                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBtnHoverColorBrush}" />
                                        </Trigger>
                                    </Style.Triggers>

                                </Style>
                            </TextBlock.Style>
                            
                        </TextBlock>

                        <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource PrimaryBorderColorBrush}"
                                Grid.Row="2" Grid.ColumnSpan="2"
                                Padding="20">
                            
                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                            
                            </Grid>
                            
                        </Border>

                    </Grid>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
        
    </Grid>

</UserControl>
