<UserControl x:Class="BlogReader.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:BlogReader.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel, IsDesignTimeCreatable=True}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Style="{StaticResource ViewHeaderTextBlock}" Text="Home" />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding BlogPostItems}">

            <ItemsControl.ItemTemplate>
                <DataTemplate>

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Padding="5" Background="{StaticResource PrimaryButtonColorBrush}">
                            
                            <TextBlock Text="{Binding SourceName}" 
                                       FontWeight="DemiBold"
                                       Foreground="#fff" />
                            
                        </Border>

                        <TextBlock Text="{Binding Title}"
                                   FontWeight="Bold"
                                   Margin="0 5 0 0" 
                                   FontSize="24"
                                   Grid.Row="1" />

                    </Grid>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>
        
    </Grid>

</UserControl>
